# Неделя 3, день 2 — персонализация

- Скопирован day-01 → day-02; добавлен слой `profiles` (`profiles.py`, JSON в `data/profiles/`).
- Три seed-профиля: Марта, доктор Клык, директор — стиль/формат/constraints подмешиваются в system prompt.
- Классификатор расширен: `layer: profile` → `learned`; эвристика на «запомни/фиксируй/всегда» как запасной путь.
- Demo: три сессии про опоссума **Лапку** (хвост), не Пушок; `[profile]` в начале каждой сессии.
- `fixed_message` в user_sim для ходов с предпочтениями — стабильный сценарий на видео.

## На видео

`python weeks/week-03/day-02/main.py --demo` — контраст тона (Марта vs Клык vs директор), сохранение в working и learned.

## Вывод

Профиль — отдельный слой рядом с памятью; без него один и тот же факт звучит одинаково для всех ролей.
