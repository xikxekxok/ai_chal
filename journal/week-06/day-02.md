# Неделя 6, день 2 — локальный CLI-чат

- Самодостаточный Python CLI: `llm.py` (Ollama HTTP), `agent.py` (история в памяти), `main.py`.
- Режимы: `--check` (без генерации), `--chat`, one-shot. Без `--demo` — по запросу пользователя.
- Опора на week-02/day-01, провайдер — Ollama (`qwen3:8b`, `OLLAMA_THINK=false`).

**На видео:** `--check`, one-shot с промптом, `--chat` с 2–3 ходами (память в сессии).
